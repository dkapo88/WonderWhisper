<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />

    <!-- Hardware features -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DictationKeyboardAI">

        <service
            android:name=".BubbleOverlayService"
            android:exported="false"
            android:foregroundServiceType="microphone">
        </service>

        <service
            android:name=".DictationAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ApiKeysActivity" />
        <activity android:name=".HowToGuideActivity"
            android:exported="false" />
        <activity android:name=".AiPromptActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".VocabularyActivity" />
        <activity android:name=".AiModelsActivity" />
        <activity android:name=".StreamingSettingsActivity" />
        <activity
            android:name=".offline.OfflineModelManagerActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".LogsActivity" />
        <activity android:name=".LogDetailActivity" />
        <activity android:name=".DictationTestActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".SimpleSettingsActivity" />
        <activity android:name=".KeyboardSettingsActivity" />
        <activity android:name=".DebugActivity" />
        <activity android:name=".AccessibilityDisclosureActivity" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".AboutActivity" android:exported="true" />

        <!-- Voice Recognition Activity - handles RECOGNIZE_SPEECH intents from other keyboards -->
        <activity
            android:name=".VoiceRecognizeActivity"
            android:exported="true"
            android:label="@string/voice_recognizer_label"
            android:launchMode="singleInstance"
            android:clearTaskOnLaunch="true"
            android:configChanges="orientation|screenLayout|screenSize|keyboardHidden|keyboard|uiMode"
            android:theme="@style/Theme.VoiceRecognizer">
            <intent-filter>
                <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".BubbleAppearanceActivity" />
        <!-- Legacy Note activities removed in favor of Compose screens -->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <!-- Minimal IME keyboard service -->
        <service
            android:name=".DictationImeService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/ime_method_config" />
        </service>

    </application>

</manifest>
